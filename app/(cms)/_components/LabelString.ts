'use client';

import styled from 'styled-components';
import { size } from '@/src/styles';

export const LabelString = styled.span`
  display: inline-block;
  font-weight: 900;
  margin-bottom: ${size.normal[2]};
`;
